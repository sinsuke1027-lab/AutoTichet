# Issue #6 #7 #13 #14 #15 — スコープフィルター共通化 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **⚠️ 前提:** `2026-06-04-issue10-project-members.md` の実装が完了していること（`project_members` テーブルが存在すること）。

**Goal:** タスク一覧・プロジェクト選択・担当者フィルターの表示範囲を、ログインユーザーが所属する部門/プロジェクトのメンバーのみに限定する。

**Architecture:** バックエンドに `src/api/routers/_scope.py` ヘルパーモジュールを新設し、`visible_user_ids()` / `visible_project_ids()` を一箇所に実装する。`tasks_crud.py`・`projects.py`・`users.py` の各エンドポイントがこのヘルパーを呼び出す形にすることで 5 Issue を一括解決する。

**Tech Stack:** SQLAlchemy 2.x, FastAPI, PostgreSQL JSONB `?|` 演算子, TanStack Query 5.x

---

## ファイルマップ

| 操作 | パス |
|-----|-----|
| 新規作成 | `src/api/routers/_scope.py` — スコープヘルパー関数 |
| 修正 | `src/api/routers/tasks_crud.py` — `list_tasks` スコープ適用 |
| 修正 | `src/api/routers/users.py` — `GET /users` に `scope` パラメータ追加 |
| 修正 | `frontend/src/hooks/useProjects.ts` — 既に Plan #10 で対応済み（変更不要） |
| 修正 | `frontend/src/pages/Calendar/index.tsx` — 担当者フィルターを `scope=visible` に切り替え |
| 新規作成 | `tests/unit/test_scope_filter.py` |

---

### Task 1: スコープヘルパーモジュールを作成する

**Files:**
- Create: `src/api/routers/_scope.py`

- [ ] **Step 1: テストを書く**

```python
# tests/unit/test_scope_filter.py
import pytest
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_list_tasks_excludes_other_dept(client: AsyncClient, auth_headers: dict) -> None:
    """タスク一覧に他部門ユーザーのタスクが含まれない（member ロール）。"""
    r = await client.get("/api/v1/tasks", headers=auth_headers)
    assert r.status_code == 200


@pytest.mark.asyncio
async def test_list_users_scope_visible(client: AsyncClient, auth_headers: dict) -> None:
    """`GET /users?scope=visible` で自分と同部門ユーザーのみ返る。"""
    r = await client.get("/api/v1/users?scope=visible", headers=auth_headers)
    assert r.status_code == 200
    users = r.json()
    assert isinstance(users, list)


@pytest.mark.asyncio
async def test_list_projects_scope_mine_default(client: AsyncClient, auth_headers: dict) -> None:
    """プロジェクト一覧はデフォルトで自分が所属するもののみ返る（#10 で実装済み）。"""
    r = await client.get("/api/v1/projects", headers=auth_headers)
    assert r.status_code == 200


@pytest.mark.asyncio
async def test_admin_sees_all_tasks(client: AsyncClient, admin_auth_headers: dict) -> None:
    """admin ロールは全タスクを参照できる。"""
    r = await client.get("/api/v1/tasks", headers=admin_auth_headers)
    assert r.status_code == 200


@pytest.mark.asyncio
async def test_visible_user_ids_includes_self(client: AsyncClient, auth_headers: dict) -> None:
    """GET /users?scope=visible に自分自身が含まれる。"""
    me_r = await client.get("/api/v1/users/me/profile", headers=auth_headers)
    my_id = me_r.json()["user_id"]
    r = await client.get("/api/v1/users?scope=visible", headers=auth_headers)
    user_ids = [u["user_id"] for u in r.json()]
    assert my_id in user_ids
```

- [ ] **Step 2: テストを実行して現状確認（一部 PASS・一部変更対象）**

```bash
pytest tests/unit/test_scope_filter.py -v
```

- [ ] **Step 3: `_scope.py` を作成する**

```python
# src/api/routers/_scope.py
"""ログインユーザーが参照できるリソース ID を返すヘルパー。

各エンドポイントから呼び出し、WHERE 句に使う。
admin ロールは全件参照可。
"""
import uuid

from sqlalchemy import select
from sqlalchemy.dialects.postgresql import array as pg_array
from sqlalchemy.ext.asyncio import AsyncSession

from src.api.auth import ROLE_HIERARCHY, CurrentUser
from src.db.models import ProjectMember, UserProfile


async def visible_user_ids(db: AsyncSession, current_user: CurrentUser) -> list[str] | None:
    """参照できるユーザー ID 一覧を返す。None は制限なし（admin）。

    対象:
    1. 自分自身
    2. 同部門タグを持つユーザー
    3. 自分が所属するプロジェクトのメンバー
    """
    user_role = max((ROLE_HIERARCHY.get(r, 0) for r in current_user.roles), default=0)
    if user_role >= ROLE_HIERARCHY["admin"]:
        return None  # admin は制限なし

    ids: set[str] = {current_user.sub}

    # 同部門タグのユーザー
    if current_user.department_tags:
        dept_result = await db.execute(
            select(UserProfile.user_id).where(
                UserProfile.department_tags.op("?|")(pg_array(current_user.department_tags))
            )
        )
        ids.update(dept_result.scalars().all())

    # 自分が所属するプロジェクトのメンバー
    proj_result = await db.execute(
        select(ProjectMember.user_id).where(
            ProjectMember.project_id.in_(
                select(ProjectMember.project_id).where(
                    ProjectMember.user_id == current_user.sub
                )
            )
        )
    )
    ids.update(proj_result.scalars().all())

    return list(ids)


async def visible_project_ids(
    db: AsyncSession, current_user: CurrentUser
) -> list[uuid.UUID] | None:
    """参照できるプロジェクト ID 一覧を返す。None は制限なし（admin）。"""
    user_role = max((ROLE_HIERARCHY.get(r, 0) for r in current_user.roles), default=0)
    if user_role >= ROLE_HIERARCHY["admin"]:
        return None

    result = await db.execute(
        select(ProjectMember.project_id).where(
            ProjectMember.user_id == current_user.sub
        )
    )
    return list(result.scalars().all())
```

---

### Task 2: `list_tasks` にスコープフィルタを適用する（#6）

**Files:**
- Modify: `src/api/routers/tasks_crud.py`

- [ ] **Step 1: `_scope` をインポートする**

`src/api/routers/tasks_crud.py` の import に追加：

```python
from src.api.routers._scope import visible_user_ids as _visible_user_ids
```

- [ ] **Step 2: `list_tasks` の既存スコープロジック（`_scope_condition`）を `_visible_user_ids` に置き換える**

`list_tasks` 関数内の `user_role` を取得して `member`/`leader` でフィルタしている既存ブロック（約 `if not my_tasks_only and user_role < ROLE_HIERARCHY["manager"]:` の箇所）を以下に差し替える：

```python
    if not my_tasks_only:
        allowed_uids = await _visible_user_ids(db, current_user)
        if allowed_uids is not None:
            # None = admin（制限なし）、list = 制限あり
            query = query.where(
                or_(
                    Task.assignee_id.in_(allowed_uids),
                    Task.visibility == "all",
                    Task.created_by == current_user.sub,
                )
            )
```

- [ ] **Step 3: テストを実行する**

```bash
pytest tests/unit/test_scope_filter.py tests/unit/test_tasks_crud_router.py -v
```

期待: 全 PASS

- [ ] **Step 4: コミットする**

```bash
git add src/api/routers/_scope.py src/api/routers/tasks_crud.py
git commit -m "feat: タスク一覧の表示範囲を同部門/プロジェクトメンバーに限定 (#6)"
```

---

### Task 3: `GET /users` に `scope=visible` パラメータを追加する（#15）

**Files:**
- Modify: `src/api/routers/users.py`

- [ ] **Step 1: `users.py` の `list_users` エンドポイントを確認する**

```bash
grep -n "def list_users\|async def list" src/api/routers/users.py
```

- [ ] **Step 2: `list_users` に `scope` パラメータを追加する**

`src/api/routers/users.py` の `list_users` 関数シグネチャを更新：

```python
from src.api.routers._scope import visible_user_ids as _visible_user_ids

@router.get("", response_model=list[AdminUserResponse])
async def list_users(
    db: DbDep,
    current_user: CurrentUser,
    scope: str = Query(default="all"),  # "all" | "visible"
) -> list[AdminUserResponse]:
    query = select(UserProfile).order_by(UserProfile.display_name)
    if scope == "visible":
        allowed_uids = await _visible_user_ids(db, current_user)
        if allowed_uids is not None:
            query = query.where(UserProfile.user_id.in_(allowed_uids))
    result = await db.execute(query)
    return [AdminUserResponse.model_validate(u) for u in result.scalars().all()]
```

- [ ] **Step 3: テストを実行する**

```bash
pytest tests/unit/test_scope_filter.py -v
```

期待: 5 件全 PASS

- [ ] **Step 4: コミットする**

```bash
git add src/api/routers/users.py
git commit -m "feat: GET /users に scope=visible パラメータを追加 (#15)"
```

---

### Task 4: カレンダービューの担当者フィルターを `scope=visible` に切り替える（#15）

**Files:**
- Modify: `frontend/src/pages/Calendar/index.tsx`

- [ ] **Step 1: カレンダーの担当者取得方法を確認する**

```bash
grep -n "useUsers\|/users\|assignee" frontend/src/pages/Calendar/index.tsx | head -15
```

- [ ] **Step 2: ユーザー取得フックに `scope` パラメータを追加する**

`frontend/src/hooks/useUsers.ts`（または類似フック）の `useUsers` 関数を確認し、`scope` パラメータを渡せるよう更新する：

```typescript
// frontend/src/hooks/useUsers.ts （既存関数を拡張）
export function useUsers(params: { scope?: 'all' | 'visible' } = {}) {
  const { scope = 'all' } = params
  return useQuery({
    queryKey: ['users', { scope }],
    queryFn: async () => {
      const { data } = await api.get('/users', { params: { scope } })
      return data
    },
  })
}
```

- [ ] **Step 3: カレンダーページの担当者取得を `scope: 'visible'` に変更する**

`frontend/src/pages/Calendar/index.tsx` でユーザーを取得している箇所を検索し、`scope: 'visible'` を追加する：

```typescript
const { data: users = [] } = useUsers({ scope: 'visible' })
```

- [ ] **Step 4: TypeScript ビルドを確認する**

```bash
cd frontend && npx tsc --noEmit
```

- [ ] **Step 5: コミットする**

```bash
git add frontend/src/pages/Calendar/index.tsx frontend/src/hooks/useUsers.ts
git commit -m "feat: カレンダービューの担当者フィルターを所属プロジェクト/部門メンバーに限定 (#15)"
```

---

### Task 5: 全テスト実行・最終確認

- [ ] **Step 1: バックエンドテストを全件実行する**

```bash
pytest tests/ -v --tb=short
```

期待: 既存テスト + 新規 5 件 = 全 PASS

- [ ] **Step 2: フロントエンドビルドを確認する**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: GitHub Issues をクローズする**

```bash
gh issue close 6 7 13 14 15 --repo sinsuke1027-lab/AutoTichet --comment "スコープフィルター共通化で一括実装完了"
```
